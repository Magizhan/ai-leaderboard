

Let me start by reading the relevant parts of the file.
Now let me see the utilisation gauge HTML section and a bit after it.
Now I have all the context I need. Let me make the three edits.
Now let me find exactly where `.lb-rank-badge` is referenced to verify the CSS was added in the right spot, and add the streak badge in `buildRowHTML`.
Good. Now add the streak badge after the rank badge in `buildRowHTML`.
Now add the billing countdown CSS and HTML. First the CSS (near the gauge styles):
